pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

plugins {
    id("org.gradle.toolchains.foojay-resolver-convention") version "0.5.0"
}

rootProject.name = "opentelemetry-gradle-instrumentation"

enableFeaturePreview("STABLE_CONFIGURATION_CACHE")
